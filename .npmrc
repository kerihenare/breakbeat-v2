# Don't abort `pnpm <script>` on the pre-run dependency check. Build-script
# approvals live in pnpm-workspace.yaml (the `allowBuilds` map); without this,
# pnpm 11 fails every script run while approvals are pending.
verify-deps-before-run=false
