# .input — scratch dir

Task-scoped working files. Not persisted, gitignored. Holds expanded prompt details, notes, intermediate artefacts for the current task only.

- Ephemeral — do not rely on contents across tasks
- Never reference from committed code
- Safe to clear between tasks
